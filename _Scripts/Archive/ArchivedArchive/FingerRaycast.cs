using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FingerRaycast : MonoBehaviour
{
    // public GameObject ray; // The ray to be toggled
    // public OVRHand hand; // The hand to be used
    // // Start is called before the first frame update
    // void Start()
    // {
    //     ray.SetActive(false); // Initially, the ray is off
    // }

    // // Update is called once per frame
    // void Update()
    // {
        
    //     if (hand.GetFingerIsPinching(OVRHand.HandFinger.Index)) // If the index finger is pinching
    //     {
    //         Ray ray = new Ray(OVRInput.GetLocalControllerPosition(OVRInput.Controller.RTouch), OVRInput.GetLocalControllerRotation(OVRInput.Controller.RTouch) * Vector3.forward);
    //         ray.SetActive(true); // Enable the ray
    //     }
    //     else
    //     {
    //         ray.SetActive(false); // Disable the ray
    //     }
    // }
}
